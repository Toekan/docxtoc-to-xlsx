from setuptools import setup, find_packages

setup(
    name='docxtoc-to-excel',
    version='beta',
    description='Transforms docx table of content into xlsx file',
    author='Toekan',
    packages=find_packages()
)